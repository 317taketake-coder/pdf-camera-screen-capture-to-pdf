# Webカタログカメラ 実行に必要なパッケージ
#
# 動作確認バージョン（2026-08）
#   Windows 11 25H2 / Python 3.x
#
# インストール:
#   pip install -r requirements.txt
#
# 注意:
#   PyInstaller でビルドする際は、必ずこの環境を有効化した状態で実行すること。
#   別の環境から固めると、バンドルされるライブラリのバージョンが変わり、
#   実行時にだけ表面化する不具合の原因になる。

img2pdf==0.6.3
Pillow>=10.0.0
